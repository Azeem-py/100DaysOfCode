// let officialName = prompt("What is javascript's official name?");

// (officialName=='ECMAScript') ? alert("Correct! you got it") : alert('Wrong! It\'s ECMAScript');

// let number = prompt("Enter a number")

// if (number > 0) alert(1);
// else if (number < 0) alert(-1);
// else if (number == 0) alert(0);

// let result;

// if (a + b < 4) {
//   result = 'Below';
// } else {
//   result = 'Over';
// }

// let a = prompt('Enter a number');
// let b = prompt('Enter a number');

// result is going to be assigned to the value that is said to be true after checking id a+b is greater than 4.. If it's greater then result will be below else Over
// result = (a+b<4) ? 'Below': 'Over'
// console.log(result)


//LOGICAL OPERATORS

//OR OPERATOR

//this finds the first truthy value in an expression for example 
// true || false --->> true because there's one truthy here
// false || false --->> false because there's no truthy here

//AND OPERATOR

//this finds the first falsy value or rather it checks if there's any falsy value at all
//unlike the OR operator it makes sure all sides are true

//true && false --->> returns false because there's a falsy here
//true && true --->> returns true because there's no falsy here


// NOT OPERATOR

//This is a operator is for opposing
//for example, let a = true; let b = !a this -->> this is going to return false because the opposite of true is false

// double exclamation mark can also be used to convert a value to boolean
//for example 
//alert(!!"This is true") -->> the value is going to be true because it's not an empty string
//alert(!!null) -->> the value is going to be false as null is false

//so basically double exlamation is not negating or opposing this time it's converting them to their real value in boolean form


